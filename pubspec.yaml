name: movie_max
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_screenutil_plus: ^1.6.0
  flutter_native_splash: ^2.4.8
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - asset/images/

flutter_native_splash:
  color: "#000000"
  image: asset/images/movie_max_logo.png

  android_12:
    color: "#000000"
    image: asset/images/movie_max_logo.png

  